let numbers = [1, 2, 3, 7, 8]
let num = 9
if (numbers.includes(num)) {
  console.log(`si se encuentra el valor ${num}`)
} else {
  console.log(`el numero ${num} no se encuentra`)
}
let base = 4
let exponent = 3
let result = base ** exponent

console.log(result)